import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Title } from './styles';

export default function OnboardingAuth() {
  return (
    <SafeAreaView>
      <Title>OnboardingAuth</Title>
    </SafeAreaView>
  );
}
