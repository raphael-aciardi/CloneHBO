import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function OnboardingAuth() {
  return (
    <SafeAreaView>
      <Text>OnboardingAuth</Text>
    </SafeAreaView>
  );
}
